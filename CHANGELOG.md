# 版本维护记录

## 维护规则

- 版本号使用 `MAJOR.MINOR.PATCH`。
- 新版本写在顶部；已发布条目不改写。
- 状态只使用：`预发布`、`稳定`、`撤回`。
- 变更只使用：`新增`、`修复`、`变更`、`已知问题`。
- 每条只写一件事，格式为“动作 + 结果”，不写过程。
- 每个版本必须记录：日期、平台、状态、提交号、安装包和 SHA256。
- 安装包命名格式：`weiyu-cay_<版本>_<平台>-<类型>.<扩展名>`。

## 版本模板

```markdown
### <版本号> · <平台> · <状态>
日期：YYYY-MM-DD
提交：<短提交号>

#### 新增
- <动作 + 结果>

#### 修复
- <动作 + 结果>

#### 变更
- <动作 + 结果>

#### 已知问题
- <问题 + 影响>

#### 安装包
- <文件名> — SHA256: `<校验值>`
```

## 版本记录

### 1.1.1 · Windows/macOS · 预发布
日期：2026-09-18
平台：Windows、macOS
状态：预发布
提交：0cd4af5

#### 新增
- 新增“每日歌词”标题栏展示，按本地日期每天稳定随机显示一条内置古代诗词，并支持在设置中开启或关闭。
- 新增本地 LaTeX 公式支持，Markdown 预览和富文档编辑器都可渲染行内、块级公式，并保留公式源码。
- 新增富文档下标、上标工具，选中文字后即可直接应用并持久化。

#### 修复
- 修复普通点击、跨文件夹切换以及欢迎页、搜索、目录和内链跳转后旧笔记仍保持绿色选中样式的问题，当前笔记与所有内嵌列表的选中状态保持一致。
- 修复切换已加载文件夹时重复读取笔记列表的问题，直接复用缓存并立即显示内容；创建、重命名、移动、删除和导入后的显式刷新仍会更新列表。
- 优化 PDF 打开性能，复用最近解析结果并共享快速切换时仍在进行的解析任务，减少冷启动字节拷贝；同时合并连续缩放操作的整页渲染，打开与缩放操作更流畅。
- 修复 Markdown 源码模式表格单元格内容被单行输入框截断且点击难以编辑的问题，改为仅在聚焦时展开的可换行编辑框，未使用时隐藏滚动条和拖拽手柄，失焦后自动收回。
- 修复 Windows 换行格式的未编辑旧版“欢迎来到微屿”笔记未能自动升级的问题，启动后更新为 v1.1.0 引导内容并保留用户编辑内容。
- 修复目录四到六级标题层级差异不明显的问题，分别增加缩进、字号和行距，便于快速定位章节。
- 修复富文档标题点击 # 后回车时内部选区滞后导致标题拆分或空行插错位置的问题，按实际光标在目标标题前插入空段落。
- 修复 macOS 上从字号输入框返回正文后新输入文字偶发丢失字号的问题，在格式应用后保留待输入的字号标记。
- 为富文档图片右键菜单增加复制图片操作，支持将图片内容写入系统剪贴板。
- 收窄图片右键菜单，仅保留与图片直接相关的复制图片操作。
- 修复富文档公式保存时被 Rust 文档校验拒绝的问题，公式节点和上下标标记现在可以持久化并重新加载。
- 调整公式入口，将其收纳到“插入”菜单中，工具栏不再单独显示公式按钮。
- 调整公式输入框示例，改用通用的变量、分式和根号示例，不再引用具体笔记内容。
- 将 Markdown 和富文档的“打印 / 保存 PDF”改为应用内直接导出，按 A4 分页保存渲染结果，减少系统打印预览造成的格式变化。
- 修复应用内 PDF 导出缺少主窗口权限导致始终失败的问题，导出命令现在可正常调用并保留原笔记。
- 修复 PDF 导出中的外部超链接不可点击的问题，为链接写入 PDF 可点击区域。
- 修复带图片的富文档导出期间保存状态同步触发编辑器重新载入的问题，导出过程保持当前编辑内容和渲染源稳定。
- 调整应用内 PDF 导出为独立的白色打印背景和中性文字颜色，不再把应用主题色带入导出文件。
- 修复 Markdown 表格导出时因长链接撑宽表格导致右侧列被截断的问题，导出时固定页面宽度并允许单元格内容换行。
- 修复 Markdown 预览外链地址存放在 data 属性时 PDF 点击区域未生成的问题，导出后链接可继续跳转。
- 修复从笔记列表导出当前富文档时无条件重新选择当前笔记的问题，避免带图文档重新载入导致导出无响应。
- 修复重复导出到同名 PDF 时被错误拒绝的问题，改为安全原子替换；不支持转换的附件不再显示无效导出入口。

- 修复 Markdown 和富文档 PDF 导出在表格行、大图处被硬切的问题，分页会尽量保持表格行和图片完整。
- 提升 PDF 导出栅格质量，改用 3 倍渲染和无损 PNG，图片与细小文字更清晰。
- 优化 PDF 导出中的长外链显示为紧凑域名，同时保留完整可点击地址，避免表格列被 URL 撑成竖条。
- 为富文档正文增加 Ctrl+滚轮缩放，范围与 Markdown 预览一致为 50% 至 200%。

- 修复富文档 PDF 中高亮文本跨行时背景与字符错位的问题，换行后的高亮范围保持完整。
- 支持在 Markdown 可编辑表格单元格中按住 Ctrl/⌘ 点击 URL 打开外链，普通点击继续用于编辑。
- 修复 PDF 分页在普通段落和高亮文本中间截断的问题，页边界会避让完整块级内容。
- 修复富文档图片尚未显示时导出 PDF 缺图的问题，导出前按持久化资源路径补载图片。
- 修复 PDF 导出后图片缺失造成的后续内容错版，并同步收紧系统打印路径的段落分页规则。
- 修复带图片文档 PDF 生成失败的问题，恢复兼容的 PNG 写入方式并保留高清渲染。
- 修复 PDF 高亮跨行背景错位，按实际文字行绘制高亮底色，避免文本换行被高亮样式干扰。
- 降低 PDF 页面栅格化开销并并行加载 Markdown 图片，改善多图文档导出和打开速度。
- 优化启动恢复失败提示，显示索引数据不完整或文件写入失败等可执行原因。
- 统一启动恢复提示条和重试按钮的微屿主题样式，避免遮挡搜索框并提供清晰焦点反馈。
- 修复 Markdown 表格点击单元格时横向滚动跳回左侧的问题，并支持 Tab/Shift+Tab 按表格顺序切换单元格。
- 优化 PDF 分页边界，只避让实际跨页的文本行，减少内容尚未结束却提前换页造成的大块留白。
- 修复 Markdown 表格编辑提交后异步重建导致横向滚动位置归零的问题，继续编辑右侧单元格时保留当前位置。
- 调整字号输入范围反馈，输入值会收敛到 8–96 磅并补充 8 和 96 的字号建议。
- 收敛富文档格式工具栏的间距、颜色和焦点样式，使控件更贴合微屿主题。
- 修复 PDF 导出中富文档缩放图片外层未居中的问题，图片位置与编辑器保持一致。
- 修复 PDF 高亮片段在中文混排时形成异常空白的问题，允许标记内容在必要处自然断行。
- 修复 Markdown 表格编辑时点击其他单元格需要重复点击的问题，提交当前单元格后一次点击即可进入目标单元格。
- 新增富文档内置图片拖动缩放，图片宽度持久化并同步影响 PDF 导出尺寸。
- 修复富文档图片宽度字段未被 Rust 保存校验允许，调整图片大小后现在可以正常保存并重新加载。
- 修复 PDF 导出未保留富文档图片缩放尺寸的问题，导出结果现在沿用当前图片宽度。
- 修复 PDF 高亮文本使用内联块布局导致中文被强制拆行和出现大块留白的问题，中文按自然排版换行。
- 修复富文档标题前缀通过删除键修改后未持久化的问题，删除 # 后会立即同步标题级别。
- 修复富文档标题前缀删除键事件偶发未进入编辑器事务的问题，改为按键时直接持久化标题级别变化。
- 修复标题前缀退格事件被浏览器默认处理的问题，在捕获阶段统一提交标题级别变更并阻止旧前缀恢复。
- 修复删除标题前缀后点击离开未同步级别的问题，失焦时会将当前 # 数量写回文档并触发自动保存。
- 修复同一标题内从前缀点击到正文时未同步级别的问题，点击标题前会先提交当前 # 数量。
- 修复删除标题前缀后点击其他内容未同步级别的问题，编辑器根节点会在跨内容点击前统一提交所有标题的 # 数量。
- 修复 Markdown 大表格横向滚动条固定在内容底部的问题，新增跟随可视区域的浮动滚动轨道。
- 修复 Markdown 表格单元格鼠标按下阻断原生选择的问题，现在可以直接选中文字。
- 修复 PDF 导出缩放图片左贴的问题，导出图片现在保持水平居中。
- 修复 PDF 高亮和中文混排的异常强制换行，减少高亮块造成的空白。
- 新增富文档正文左对齐、居中、右对齐、两端对齐，以及字体和字号格式，并持久化到文档。
- 修复 Markdown 表格在未发生横向溢出时仍显示滚动条的问题，并让浮动滚动条不再遮挡最后一行。
- 修复 Markdown 单元格提交后横向滚动位置回到最左侧的问题，编辑右侧单元格时保留当前位置。
- 优化 PDF 分页保护策略，避免大块表格或图片导致夸张空白；高亮混排允许中文自然换行。
- 新增富文档正文和表格单元格的图标对齐工具，支持四种正文对齐和三种单元格对齐。
- 新增 Word 式数字字号输入框和数字字号建议下拉，支持 8–96 磅并兼容旧字号标记。
- 修复左侧 Markdown 条目重复显示“MD”的问题，类型标签现在只显示“Markdown”。
- 为当前笔记目录增加按标题层级折叠功能，折叠按钮仅在悬停或键盘聚焦时显现，标题点击仍用于跳转，并按笔记分别保存折叠状态。
- 调整目录层级折叠图标为上下方向的轻量 chevron，区分展开与折叠状态，避免与侧栏收起图标混淆。
- 修复富文档标题前缀未编辑时仍显示以及点击后光标跳到末尾的问题，阅读状态隐藏 #，获得焦点后可逐个编辑并即时同步标题级别。
- 优化富文档标题编辑态跟踪，改用单一选区装饰，减少大文档加载时的重复 DOM 监听。
- 修复设置弹窗关闭按钮的内外盒对齐，关闭图标现在保持稳定的水平和垂直居中。
- 优化笔记打开速度，复用最近 8 篇已加载正文并在后台重新校验，同时减少当前笔记目录的重复解析。
- 修复富文档 Markdown 标题快捷输入，输入 # 到 ###### 后离开当前段落会转换为对应层级标题，使用 \\# 可保留为普通文字。
- 调整富文档和 Markdown 各级标题字号梯度，六级标题不再小于正文。

#### 变更
- 扩展欢迎笔记升级兼容范围，覆盖 v1.1.0 前已发布的引导文案变体。
- 调整 PDF.js 失败回退测试的总超时，使冷启动环境能完整执行原有断言。
- 移除与临时 pnpm 存储目录不匹配的 CI 缓存设置，避免跨平台构建在清理阶段被误判失败。

#### 已知问题
- 安装包与 SHA256 待发布构建生成后补录。

#### 安装包
- 待发布构建生成后补录。

### 1.1.0 · Windows/macOS · 稳定
日期：2026-09-12
提交：0dae371

#### 新增
- 新增富文档笔记，支持标题、行内格式、列表、任务、链接、图片、表格和合并单元格。
- 新增文本、PDF、图片和 Office 文件条目，并保留原始文件扩展名与安全的外部打开操作。
- 新增文件夹上下文菜单、嵌套文件夹、拖放导入、键盘多选、批量移动和可恢复回收站操作。
- 新增富文档导出 DOCX 与打印保存 PDF，保留中文、图片、表格跨度和常用格式。

#### 修复
- 修复富文档自动保存的版本冲突处理，保存后继续保留当前光标、选区和滚动位置。
- 修复 Markdown 仅输入有序列表标记后按回车会丢失序号的问题，保留空列表项并继续编辑。
- 修复富文档编辑器加载外部权威版本时跳回顶部的问题，重试和载入最新版本都会保留当前草稿。
- 修复表格插入与编辑交互，支持行列选择、矩形拖动选区、整表选择、合并拆分、受限单元格内容滚动和悬停显示滚动条。
- 修复左侧笔记拖拽排序，按目标行上下半区插入并显示明确的放置位置。
- 修复非当前文件夹笔记排序后的列表刷新，拖拽完成后立即显示持久化顺序。
- 修复 DOCX 转换，保留嵌入图片、行列跨度、列宽、常用文字样式、对齐和支持的单元格颜色；转换失败时回滚未完成的副本。
- 修复持久化顺序，先成功写入正文和附件，再更新 SQLite 索引，失败时保留上一份有效内容。
- 修复 PDF 阅读器控件与页面容器的主题样式，将工具栏移到正文下方并吸附在底部，同时支持按住 Ctrl 滚轮调整缩放。
- 修复 PDF 首屏打开等待过长的问题，预加载阅读器模块并避免重复复制文件数据。
- 修复大文件预览的 IPC 传输开销，PDF 和图片改用二进制响应读取，减少打开等待。
- 修复 PDF 文件读取阻塞原生界面的问题，将索引和磁盘读取移到后台线程。
- 修复 PDF 预览缓存复用失效数据的问题，避免 Worker 转移缓冲区后再次打开失败。
- 修复 PDF 二进制预览解析失败时无法打开的问题，自动回退到兼容读取通道。
- 修复 PDF 解析兼容性，使用 legacy 阅读构建并在二进制通道异常时回退到稳定读取。
- 修复 macOS 文件导入与导出路径解析，兼容系统临时目录别名并保持源文件安全校验。
- 修复稳定发布基线读取，跟随 GitHub Release 重定向并正确识别 v1.0.2 更新清单。

- 修复重复打开同一版本的 PDF 和图片时重复读取文件的问题，会话内复用最近的预览数据。
- 修复 Markdown 预览普通 URL 的兼容处理，直接粘贴的安全链接也支持按住 Ctrl（macOS 为 Command）点击后打开。
- 修复 Markdown 预览外部链接的交互，普通点击不跳转，按住 Ctrl（macOS 为 Command）点击后打开。
- 修复富文档粘贴 URL 后的链接处理，自动识别安全地址并要求按住 Ctrl（macOS 为 Command）点击后打开。
- 优化外链交互提示，Markdown 和富文档链接悬停时显示可点击鼠标指针。


#### 变更
- 将“字号”设置明确为“界面字号”，应用界面文字随设置缩放，Markdown、纯文本、富文本和便笺正文保持独立阅读字号。
- 将文档笔记标题、标签、工具栏和表格操作统一为微屿主题样式，并压缩笔记列表行高。
- 将 Markdown 与富文档的插入菜单、右键菜单和内部链接行为统一到同一组操作。
- 将文件卡片和资料库列表改为按格式显示主题化图标，PDF 图标直接标注 PDF。
- 将当前笔记目录标题改为自动换行，完整展示过长的小标题。
- 将依赖锁文件统一指向 npm 官方 registry，使发布构建通过供应链校验。
- 将 CI 与发布工作流的 pnpm 版本升级至 11.21.0，修复 Windows runner 安装路径错误。
- 将 CI 依赖安装目录固定到 runner 临时目录，避免 Windows 默认 store 路径失效。
- 将导出测试临时目录规范化，确保 macOS 路径安全校验与正式导出行为一致。
- 将版本更新集中维护在仓库根目录 CHANGELOG.md，发布说明文件不再记录版本变更。
- 在更新设置中说明开发版未配置更新源，避免将开发环境检查失败误认为线上更新故障。

#### 已知问题
- 暂无。

#### 安装包
- latest.json — SHA256: `7d8bbec7711ef435b96843dbb01be44a23edbfd12987a188c45da5ebd850a8a7`
- SHA256SUMS — SHA256: `2ef63d56494cc72de35bf7d15f75e73a1f699f5626b3344c4a9532c7ca43af4c`
- _1.1.0_aarch64.app.tar.gz — SHA256: `fd22665ec48bd95deaa8b40aee775fa56f695d638c3d93a70cb7913aa369b8d2`
- _1.1.0_aarch64.app.tar.gz.sig — SHA256: `ddb466a3b1f9f01c33cd9344d24c7497011d191216151562c89bdc5a2e6b177c`
- _1.1.0_aarch64.dmg — SHA256: `df5e62687de0e2d14e3b5ed9a13eb92711407e17f841310580ff2278f6e931cd`
- _1.1.0_x64-setup.exe — SHA256: `9e37c552cc1d33a1e224357721d7aa90050c3ca1b8c58354ab242917bedbd559`
- _1.1.0_x64-setup.exe.sig — SHA256: `7c4c46899b6855cb07df2e057dfcb66d6a3bedcb7ebcda6c68ae760be91a2f70`
- _1.1.0_x64.app.tar.gz — SHA256: `25499c888a2de5bba15b5d88f5d33a04b761a256f788ebd087a2f56dfc6e114f`
- _1.1.0_x64.app.tar.gz.sig — SHA256: `4bf3761c4108db8aed448d70c053c81b3d55e688537b2553cbb696692baf1d1d`
- _1.1.0_x64.dmg — SHA256: `7e21dea9bb59b59cc12bff450a0486fe52483c724f85c779a0c8a8ecce5a1f8b`
- _1.1.0_x64_en-US.msi — SHA256: `145bc0a7e62c48c3b5d750c71afddc202989884d87f3f1b2f5b709c7b51d549d`
- _1.1.0_x64_en-US.msi.sig — SHA256: `def4dfe55bf17f3e7f6278775369ed8a5930022cf155d511e7c0864650da52f0`


### 1.0.2 · Windows · 预发布
日期：2026-09-01
提交：待发布

#### 新增
- 新增系统托盘驻留与原生右键菜单，关闭主窗口后可从托盘恢复，并可新建便笺、打开便笺收件箱、打开设置或安全退出。
- 新增首次关闭方式说明与可持久化的“关闭主窗口时隐藏到托盘”设置；托盘不可用时保持主窗口可访问。
- 新增单实例唤醒，再次启动微屿时恢复已有主窗口，避免重复托盘和并发写入进程。

#### 修复
- 将文档编辑模式中的表格作为原子块保护，避免在表格源码位置输入时退回 Markdown，并隐藏该位置的“＋”按钮。
- 在标题行编辑期间显示 Markdown 标记，按回车或将光标移出标题后再隐藏标记并显示标题样式。
- 将 Markdown 插入菜单锚定到“＋”按钮或右键点击的文档位置，滚动时跟随内容并在锚点离开视口后关闭。

#### 已知问题
- 原生托盘、Explorer 重启和多显示器等 Windows 实机验收尚未完成；安装包尚未生成，当前记录仅对应源码候选版本。

#### 安装包
- 待发布构建生成后补录文件名和 SHA256。

### 1.0.1 · Windows · 预发布
日期：2026-09-01
提交：`b95f1be`

#### 新增
- 在编辑区右键菜单和“＋”菜单中增加可搜索的内部链接弹窗。
- 在笔记标题的最后编辑时间旁显示已有标签。
#### 修复
- 按 Markdown 语法提取笔记目录，排除代码块中的示例标题并保持跳转位置一致。
- 取消文件夹内笔记列表的独立滚动和高度限制，使笔记平铺并随资料库整体滚动。
- 避免显示便笺时的窗口回调等待存储锁，减少窗口显示停顿。
- 将便笺窗口创建和显示移出主线程，避免阻塞界面响应。
- 合并同一便笺的并发显示请求并显示等待状态，避免连点触发重复窗口错误。
- 移除折叠目录上的笔记数量，仅保留“目录”文字。
- 移除笔记行悬停删除按钮，保留右键菜单和键盘删除入口。
- 将删除撤销提示改为底部浮层，避免挤占笔记列表空间。
- 为新增表格列分配未占用的默认名称，保留已有表头与内容。
- 移除移动和新建笔记菜单中残留的“未归档笔记”选项，仅提供实际文件夹。
- 移动笔记后同步更新来源与目标文件夹列表，无需再次点击文件夹刷新。

#### 变更
- 将界面及当前产品说明中的“临时便签”统一为“临时便笺”。
- 将临时便笺的默认全局快捷键调整为 `Ctrl+Shift+D`，并同步更新新手引导。
- 扩充“欢迎来到微屿”的 Markdown 与临时便笺说明，明确分隔线示例并精简结尾，同时安全升级未编辑的旧版引导内容。
- 为选区格式按钮增加对应的悬停文字提示。

#### 已知问题
- 安装包为未签名版本，首次运行可能触发 Windows SmartScreen 提示；macOS 平台验证待完成。

#### 安装包
- `weiyu-cay_1.0.1_x64-setup.exe` — SHA256: `9614C9438E13D3C7EA916EB91DF0A515BD502CA0C40334EE63B2306B6D5277B6`
- `weiyu-cay_1.0.1_x64_en-US.msi` — SHA256: `6902C03D8C4BA44377513E886E68C50AE9C6339F745C394FE36472E3E173D837`

### 1.0.0 · Windows · 撤回
日期：2026-08-31
提交：`0d165e3`
旧 Release 已删除；源码标签仍保留：[`windows-v1.0.0`](https://github.com/Tiome-tt/weiyu-cay/tree/windows-v1.0.0)

#### 新增
- 首次启动创建“开始使用”文件夹和“欢迎来到微屿”教学笔记。
- 支持 Markdown 编辑、源码/分栏/预览视图、搜索、标签和笔记链接。
- 增加临时便签和回收站。

#### 修复
- 移除未归档笔记入口。
- 根目录为空时不显示文件夹空状态提示。

#### 变更
- 将“临时收集箱”统一命名为“临时便签”。

#### 已知问题
- 安装包未签名，首次运行可能触发 Windows SmartScreen 提示。

#### 安装包
- `weiyu-cay_1.0.0_x64-setup.exe` — SHA256: `C856433D2FEEB9D97F630D0D417081B11893FE335C1A9A0E90BF6F7A54AAAB25`
- `weiyu-cay_1.0.0_x64_en-US.msi` — SHA256: `A7A71DA0A13E7B903AA86F77E369A97CE5067B0FD84B728CFBF9435D7647E084`
